version: "2"
services:
  tgnotifier:
    image: sethrah/telegramdockernotifications:1.0
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - TELEGRAM_NOTIFIER_BOT_TOKEN=...
      - TELEGRAM_NOTIFIER_CHAT_ID=-...
    ports:
      - 1337:1337
